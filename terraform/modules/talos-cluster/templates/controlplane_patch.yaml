machine:
  type: controlplane
  network:
    hostname: ${hostname}
    interfaces:
      - deviceSelector:
          physical: true
        addresses: [${ip_addr}/24]
        vip:
          ip: ${talos_vip}